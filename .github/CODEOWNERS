# CODEOWNERS — required reviewers for security-sensitive paths in juvera-sdk.
#
# After this file is synced to Juvera-AI/juvera-python via `git subtree split`,
# GitHub uses it to require approval from listed owners before PRs touching
# these paths can be merged.
#
# Lives in the SDK subtree (open-source/sdk-python/.github/CODEOWNERS) so it
# survives every sync.
#
# IMPORTANT: The @handles below match the GitHub usernames of admins on the
# Juvera-AI/juvera_ai monorepo (verified via gh api repos/Juvera-AI/juvera_ai/collaborators).

# Workflows + packaging — anything that affects how the package is built
# or published. PRs to these paths must be approved by a maintainer.
.github/                    @RocketRaccoon @vinayakraja
pyproject.toml              @RocketRaccoon @vinayakraja
MANIFEST.in                 @RocketRaccoon @vinayakraja

# Vendored data (pricing catalog) — silent changes to numbers here would
# mis-report ROI for every user.
juvera_sdk/data/            @RocketRaccoon @vinayakraja
